<table>
  <%% @users.each do |user| %>
    <tr>
      <td><%%= user.username %></td>
      <td><%%= link_to pl(:role), edit_roles_user_path(user) if can? :edit_roles, user %></td>
    </tr>
  <%% end %>
</ul>
